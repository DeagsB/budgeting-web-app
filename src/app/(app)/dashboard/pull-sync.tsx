// The pull-to-sync gesture is shared with the transactions page; this re-export
// keeps the existing `DashboardPullSync` import working.
export { PullToSync as DashboardPullSync } from '@/components/pull-to-sync'
